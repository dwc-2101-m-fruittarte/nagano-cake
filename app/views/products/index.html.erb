
<div class="container">
  <h2>商品一覧<span>（全<%= @products.count%>件）</span></h2>
  <div class="row">
    <div class="col">
      <div id="products-space">
        <% @products.each do |product| %>
	      <div class="products-lineup">
	        <%= link_to customer_product_path(product.id) do %>
            <div class="product-img">
              <%= attachment_image_tag(product, :image, size: "120x120", fallback: "no_image.jpg") %>
            </div>
            <p class="product-name">
              <%= product.name %>
            </p>
            <p class="product-price">
              ¥<%= (product.price * 1.1).to_i.to_s(:delimited) %>
            </p>
          <% end %>
        </div>
	      <% end %>
	    </div>g
	  </div>
	</div>
</div>
